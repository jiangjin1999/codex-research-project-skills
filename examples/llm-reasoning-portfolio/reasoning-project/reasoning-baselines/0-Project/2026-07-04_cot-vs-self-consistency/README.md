# cot-vs-self-consistency

First attempt: compare greedy Chain-of-Thought against Self-Consistency (majority vote over sampled chains) on a frozen GSM8K split. Keep raw outputs out of git; commit only aggregate reports.
