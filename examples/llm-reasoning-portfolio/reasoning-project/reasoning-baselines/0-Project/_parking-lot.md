# Attempt Parking Lot

Future attempt ideas live here until they are concrete enough for their own `0-Project/<date>_<name>/` folder.

| Idea | Why remember it | Trigger to revisit | Possible inputs | Promotion condition |
| --- | --- | --- | --- | --- |
| `chain-length-control` | CoT trace length may affect accuracy and cost | revisit after baseline metric is frozen | CoT baseline prompt, frozen GSM8K split | promote when there is a clear metric and run budget |
