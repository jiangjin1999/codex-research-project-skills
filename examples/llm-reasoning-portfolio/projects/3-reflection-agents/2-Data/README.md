# Data

Benchmark notes for this project. Public boundary: dataset names and aggregate metrics only.

| Source | Access class | Version | Role | QC state | Public boundary |
| --- | --- | --- | --- | --- | --- |
| (shared) GSM8K / StrategyQA | open | TBD | reuse frozen baseline set | inherit from 1-reasoning-baselines | name + aggregate metrics |

Reuse the `1-reasoning-baselines` frozen set for a fair comparison. No raw records here.
