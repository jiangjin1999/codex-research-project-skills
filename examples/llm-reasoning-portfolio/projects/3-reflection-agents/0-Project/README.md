# 0-Project

Concrete attempts. One folder per attempt: `YYYY-MM-DD_short-name/`.

No attempt started yet — the project is in literature-framing. The first attempt will compare a task with and without a reflection loop on a shared benchmark.
